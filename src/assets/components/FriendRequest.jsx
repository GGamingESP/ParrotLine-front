// FriendRequests.js
import React, { useState } from "react";

function FriendRequests({ friendRequests, handleAcceptRequest, handleRejectRequest }) {
  // ... tu lógica para las solicitudes de amistad

  return (
    <dialog id="modal_2" className="modal">
      {/* Contenido de las solicitudes de amistad pendientes */}
      {/* ... */}
    </dialog>
  );
}

export default FriendRequests;
